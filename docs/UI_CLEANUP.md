* Make Sqliter UI Full Screen by default. 
* Make Sqliter Icon the flame emoji. 
* Remove all the existing Buttons from the Sqliter UI  And use mac native buttons.
* Update the Banquet Bar to display the banquet url of the table displayed in the Sqliter UI, when it launches and at all times. 
* Remove success alert at bottom of window. 
