!!!! Design improvements
!!!! Since the client is a flutter app we can use the pagination features of the grid. and parse the #anchor to be page number.

!!!! This isn't a fix. It's new work. If we can get th basics working. Invent Query Style.  Default limit, offset, sort, filter of rows, filter of columns.  Allow for query style as granular as specific tables. Use the lessons learned from SwiftUI and Tailwind CSS to design style correctly. 
Ideas for query style: 
auto hide columns where values are null. 
autohide columns that are for internal use like created, updated, deleted 
With data about usage, make better decisions about which columns to show and hide, and column widths. 

